// app_colors.dart

import 'package:flutter/material.dart';

// --- APPLICATION COLOR PALETTE ---

class AppColors {
  // Primary/Accent Colors
  static const Color kDarkBlue = Color(0xFF415e95);    // Menu bar, Primary buttons, Headings (dark blue).
  static const Color kLightBlue = Color(0xFF98b0d8);   // Form sections, Highlight backgrounds (light blue)
  static const Color kPastelBlue = Color(0xFFc9d8ed);   // Input field fills, Subtle backgrounds (pastel blue)
  static const Color appbarblue = Color(0xFF0A3D91);

  // Utility/Neutral Colors
  static const Color kGrey = Color(0xFFc9c9c9);        // Borders, Dividers, Separators (grey)
  static const Color kWhite = Colors.white;            // Default background/text color
  static const Color kBlack = Colors.black;            // Default text color

  // Semantic Colors
  static const Color kErrorRed = Colors.red;           // For errors/alerts
  static const Color kSuccessGreen = Colors.green;



  static const kDarkText = Color(0xFF415e95);   // For success messages
}